import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taskorganizer.app',
  appName: 'Task Organizer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
